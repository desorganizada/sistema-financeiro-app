import 'package:flutter/material.dart';

class AppColors {
  // Cores principais - Terracota
  static const Color primaryTerracota = Color(0xFFC75C3C);      // Terracota principal
  static const Color lightTerracota = Color(0xFFE08E6A);       // Terracota claro
  static const Color softTerracota = Color(0xFFF4C2A1);        // Terracota suave
  static const Color veryLightTerracota = Color(0xFFFFE4D6);   // Terracota muito claro
  
  // Cores de texto - Marrom
  static const Color textDark = Color(0xFF4A3B32);             // Marrom escuro
  static const Color textMedium = Color(0xFF8B7355);           // Marrom médio
  static const Color textLight = Color(0xFFC4B5A5);            // Marrom claro
  
  // Cores de fundo - Areia
  static const Color backgroundSand = Color(0xFFFDF8F5);       // Areia rosada fundo
  static const Color surfaceSand = Color(0xFFFFFFFF);          // Branco
  static const Color cardSand = Color(0xFFFFFFFF);             // Branco para cards
  static const Color borderSand = Color(0xFFF0E4D8);           // Bege escuro para bordas
  
  // Cores de status
  static const Color success = Color(0xFF2E8B57);              // Verde marinho
  static const Color warning = Color(0xFFE6A647);              // Dourado
  static const Color error = Color(0xFFD64545);                // Vermelho terracota
  static const Color info = Color(0xFF5B8FB9);                 // Azul acinzentado
  
  // Cores de ícones
  static const Color iconPrimary = Color(0xFFC75C3C);          // Terracota
  static const Color iconSecondary = Color(0xFFE08E6A);        // Terracota claro
  static const Color iconDisabled = Color(0xFFF0E4D8);         // Bege escuro
  
  // Destaques e sombras
  static const Color highlight = Color(0xFFFFE4D6);            // Terracota muito claro
  static const Color shadow = Color(0xFFE8DCD0);               // Sombra suave
}